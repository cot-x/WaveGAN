# WaveGAN
Custmized WaveGAN.

## python WaveGAN.py --help
```
usage: WaveGAN.py [-h] [--sound_dir SOUND_DIR] [--result_dir RESULT_DIR] [--weight_dir WEIGHT_DIR] [--sound_length SOUND_LENGTH]
                  [--feed_dim FEED_DIM] [--lr LR] [--mul_lr_dis MUL_LR_DIS] [--batch_size BATCH_SIZE] [--num_train NUM_TRAIN] [--cpu]
                  [--generate GENERATE] [--noresume]

options:
  -h, --help            show this help message and exit
  --sound_dir SOUND_DIR
  --result_dir RESULT_DIR
  --weight_dir WEIGHT_DIR
  --sound_length SOUND_LENGTH
  --feed_dim FEED_DIM
  --lr LR
  --mul_lr_dis MUL_LR_DIS
  --batch_size BATCH_SIZE
  --num_train NUM_TRAIN
  --cpu
  --generate GENERATE
  --noresume
```
